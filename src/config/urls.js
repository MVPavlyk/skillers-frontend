const badeURL = 'http://localhost:1337/api';

export default badeURL;

export const urls = {
    tests: '/tests',
    techNames: '/tech-names',
    exercises: '/exercises?populate=%2A&filters[testId][$eq]='
};